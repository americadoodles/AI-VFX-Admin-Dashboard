# =============================================================================
# AI VFX Admin Dashboard – Backend Environment Variables
# =============================================================================

# PostgreSQL connection string (Google Cloud SQL)
# For local dev via Cloud SQL Auth Proxy:
#   DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/ai_vfx
# For direct Cloud SQL connection:
#   DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@CLOUD_SQL_IP:5432/ai_vfx
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_vfx

# JWT signing secret – MUST be changed in production
SECRET_KEY=your-secret-key-change-in-production

# Staff token expiration (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Connection pool settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30

# Google Cloud SQL Auth Proxy connection name (optional)
# Format: project:region:instance
CLOUD_SQL_CONNECTION_NAME=
